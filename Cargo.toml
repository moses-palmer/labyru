[package]
name = "labyru"
version = "0.1.0"
authors = ["Moses Palmér <mosespalmer@gmail.com>"]

[dependencies]
clap = "2.24.1"
image = { version = "0.15", optional = true }
rand = "0.3"
rayon = { version = "0.8.2", optional = true }
svg = { version = "0.5", optional = true }

[features]
default = ["background", "parallel", "render-svg"]
background = ["image"]
parallel = ["rayon"]
render-svg = ["svg"]
